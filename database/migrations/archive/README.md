# Archived PPMF Legacy Migrations

These migration files were moved out of the active migration path on **2026-06-22** as part of the PPMU KPI dashboard cleanup.

They created tables for the old PPMF portal (inspections, geo-tagging, baseline, CM scorecard) that are **not used** by the current PPMU app.

**Do not move back** unless you intentionally restore the legacy PPMF modules.

Active PPMU schema is defined by the remaining files in `database/migrations/`.
